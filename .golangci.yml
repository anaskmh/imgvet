version: "2"

linters:
  settings:
    errcheck:
      # Terminal/report writers: a failed print surfaces via the final Flush
      # or Write error; checking every Fprint call is pure noise.
      exclude-functions:
        - fmt.Fprint
        - fmt.Fprintf
        - fmt.Fprintln
